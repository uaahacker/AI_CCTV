Django==5.0.6
djangorestframework==3.15.1
djangorestframework-simplejwt==5.3.1
django-cors-headers==4.3.1
django-filter==24.2
drf-spectacular==0.27.2
psycopg[binary]==3.1.19
celery==5.4.0
redis==5.0.4
python-dotenv==1.0.1
cryptography==42.0.7
gunicorn==22.0.0
whitenoise==6.7.0

# Observability — Sentry SDK + Prometheus metrics exporter. Both are no-ops
# until SENTRY_DSN / PROMETHEUS_METRICS_ENABLED env vars are set.
sentry-sdk[django,celery]==2.11.0
prometheus-client==0.20.0

# Test stack — only used in CI (see .github/workflows/ci.yml).
pytest==8.3.2
pytest-django==4.8.0
pytest-cov==5.0.0
factory-boy==3.3.0
